version: "3.9"

services:
  chatdesk-ai:
    build: .
    container_name: chatdesk-ai-server
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000
